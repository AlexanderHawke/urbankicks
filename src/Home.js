

const Home = () => {

    return ( 
        <div className="home">
            <p>Home screen!</p>
            {/* <BlogList blogs={blogs.filter((blog) => blog.author === 'mario')} title = "Mario's Blogs:" handleDelete = {handleDelete}/> */}
        </div>
    );
}
 
export default Home;